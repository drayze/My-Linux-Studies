Function Test:

1. To test the functionality, I added several directories.  
2. To these directories, I added several files.
3. Employed the sudo command, to changed my password. To remove
        a typo.
4. Enabled SSH.  Then retrieved the IPv4 address.  
5. Returned to the host and opened a terminal.  Using PowerShell
        with the SSH username@theIPv4 command, established a remote
        connection.  Upon entering the password, gained full access.
6. From the SSH connection, created another directory with a 
        subdirectory.  Then created an empty file called notes.txt
        inside of the subdirectory.
7. Changed present working directory back to home/user/ and created 
        a symbolic link pointing to notes.txt.  
8. Created a hard link to to an existing file, and added some content.
9. Ran 'cat' on the original file to test the hard link and its new content. 